version: 1.0

build:
  builder: oryx
  language: python
  python_version: 3.10
  requirements_file: requirements.txt

run:
  command: streamlit run nba_app.py --server.port=8000 --server.address=0.0.0.0

