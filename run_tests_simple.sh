# Quick Test Runner (No Virtual Environment)
# Use this if you already have dependencies installed globally or in another env

echo "🧪 Running tests without venv setup..."
echo ""

# Run pytest directly
python3 -m pytest "$@"
